// 23/05/2025 14:42 • 
using NUnit.Framework;
using NUnitTestProject1;
using OpenQA.Selenium;
using OpenQA.Selenium.Chrome;
using OpenQA.Selenium.Support.UI;
using System;
using System.Collections.Generic;

namespace Tests
{
    // Nuget install: Selenium.WebDriver
    // Install-Package DotNetSeleniumExtras.WaitHelpers
    [TestFixture]
    public class QA_Site_Tests
    {
        private IWebDriver driver;
        private IJavaScriptExecutor js;

        QA_POM qa;

        [SetUp]
        public void SetUp()
        {
            driver = new ChromeDriver();
            js = (IJavaScriptExecutor)driver;

            qa = new QA_POM(driver);

        }
        [TearDown]
        protected void TearDown()
        {
            driver.Quit();
        }
        [Test]
        public void checkNumberOfJavaCoursesIs192()
        {
            qa.OpenandResizeWebsite("https://www.qa.com/");
            string title = (string)js.ExecuteScript("return document.title;");
            // (string)js.ExecuteScript("document.getElementById('my element ID').innerHTML='AAAAAAAA'");
            qa.clickCookiesButton();
            qa.doSearch("java");
            Assert.That(qa.getElementByName("number").Text, Is.EqualTo("192"));
        }

   
        //public IWebElement getElement_depricated(string css)
        //{
        //    WebDriverWait wait = new WebDriverWait(driver, TimeSpan.FromSeconds(10));
        // 
        //    // Wait until element is visible
        //    IWebElement element = wait.Until(ExpectedConditions.ElementIsVisible(By.CssSelector(css)));
        //                          ElementIsVisible, ElementExists, ElementToBeClickable, InvisibilityOfElementLocated 
        //    return element;
        //}

    }

}
//===============C#=============POM===POM==-=-=-=-==
using OpenQA.Selenium;
using OpenQA.Selenium.Support.UI;
using System;

namespace NUnitTestProject1
{
    class QA_POM
    {
        private IWebDriver driver;

        public QA_POM(IWebDriver driver)
        {
            this.driver = driver;
        }


        public void doSearch(string searchText)
        {
            IWebElement search = driver.FindElement(By.Name(searchText));
            search.Click();
            search.SendKeys("java" + Keys.Enter);
        }

        public void clickCookiesButton()
        {
            getElement("#CybotCookiebotDialogBodyButtonAccept").Click();
        }

        public void OpenandResizeWebsite(String url)
        {
            driver.Navigate().GoToUrl(url);
            driver.Manage().Window.Size = new System.Drawing.Size(1552, 832);
        }

        public IWebElement getElement(string css)
        {
            WebDriverWait wait = new WebDriverWait(driver, TimeSpan.FromSeconds(10));
            IWebElement element = wait.Until(driver =>
            {
                var el = driver.FindElement(By.CssSelector(css));
                return el.Displayed ? el : null;
            });
            return element;
        }
        public IWebElement getElementByName(string name)
        {
            WebDriverWait wait = new WebDriverWait(driver, TimeSpan.FromSeconds(10));
            IWebElement element = wait.Until(driver =>
            {
                var el = driver.FindElement(By.Name(name));
                return el.Displayed ? el : null;
            });
            return element;
        }
    }
}
===+++===+++===+++===+++===+++===+++===+++===+++===+++===+++===+++

JAVA
JAVA
JAVA!


---------------------------
// 23/05/2025 
package seleniumProject;
import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
//Generated by Selenium IDE
import static org.hamcrest.CoreMatchers.is;
import static org.hamcrest.core.IsNot.not;
import java.util.*;
import java.net.MalformedURLException;
import java.net.URL;
import java.time.Duration;
import org.openqa.selenium.By;
import org.openqa.selenium.Dimension;
import org.openqa.selenium.JavascriptExecutor;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;
import org.openqa.selenium.By;
public class Test1 {
private WebDriver driver;
private Map<String, Object> vars;
JavascriptExecutor js;
public Test1() {
    driver = new ChromeDriver();
    js = (JavascriptExecutor) driver;
    vars = new HashMap<String, Object>();
    start();
}

public void tearDown() {
    driver.quit();
}

@Test
public void start() {
    driver.get("https://www.qa.com/");
    driver.manage().window().setSize(new Dimension(1536, 816));
    driver.findElement(By.cssSelector(".header__newSearch > span")).click();
    driver.findElement(By.cssSelector(".fc59__input")).click();
    driver.findElement(By.cssSelector(".fc59__input")).sendKeys("java");
    driver.findElement(By.cssSelector(".fc59__button > svg")).click();
    String res = driver.findElement(By.cssSelector(".search__resultsSectionTitle i")).getText();

// vars.put("java_count", driver.findElement(By.cssSelector(".search__resultsSectionTitle i")).getText());
}
public WebElement getElement(String css)
{
    Duration duration = Duration.ofSeconds(10);
    WebDriverWait wait = new WebDriverWait(driver, duration);

    // Wait until element is visible
    WebElement element = wait.until(
        ExpectedConditions.elementToBeClickable(By.cssSelector(css)));

    return element;
    /*
     * ElementIsVisible(By by)
     * ElementExists(By by)
     * ElementToBeClickable(By by)
     * InvisibilityOfElementLocated(By by)
     */
    
}

}


<project xmlns="http://maven.apache.org/POM/4.0.0"
xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
<modelVersion>4.0.0</modelVersion>
<groupId>com.qa</groupId>
<artifactId>seleniumProject</artifactId>
<version>0.0.1-SNAPSHOT</version>
<dependencies>
    <dependency>
        <groupId>org.seleniumhq.selenium</groupId>
        <artifactId>selenium-java</artifactId>
        <version>4.20.0</version> <!-- Check for latest version -->
    </dependency>
    <dependency>
        <groupId>io.github.bonigarcia</groupId>
        <artifactId>webdrivermanager</artifactId>
        <version>5.8.0</version> <!-- Or latest -->
    </dependency>

</dependencies>

</project>
